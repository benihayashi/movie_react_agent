###Task:
Extract the entities and the movie genre from the following movie description:

!<INPUT_0>

###Entities to extract:
locations, characters, time period, events, sentimental, genre !<INPUT_1>

###Example output
{"characters": ["jack", "ben"]}

###Output format
json format,  the key for each properties should be the exracted entity (e.g. locations, characters), and the value should be array of string, you may put an empty array for fields that have no values